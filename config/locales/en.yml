en:
  date:
    formats:
      default: '%m/%d/%Y'
      with_weekday: '%a %m/%d/%y'

  entry:
    errors:
      date_in_future: "can't be in the future"

  time:
    formats:
      default: '%a, %b %-d, %Y at %r'
      date: '%b %-d, %Y'
      short: '%B %d'

  txn:
    errors:
      date_does_not_match_parent: "must match parent transaction date"
      unbalanced: "debits and credits must balance"
