#main
  .scroll-panel
    .scroll-panel-header
      = link_to "#", class: "btn btn-default btn-sm pull-left visible-xs", data: { toggle: "off-canvas", target: "#target" } do
        %i.fa.fa-bars
      = link_to Account, class: "btn btn-primary btn-sm pull-right" do
        Cancel
      %h3.scroll-panel-title Edit Account
    .scroll-panel-body
      = render 'form'
